export const ROLES = Object.freeze({
    ADMIN: 1,
    VET: 2,
    USER: 3,
});

export const ROLE_NAMES = Object.freeze({
    [ROLES.ADMIN]: 'admin',
    [ROLES.VET]: 'vet',
    [ROLES.USER]: 'user',
});
